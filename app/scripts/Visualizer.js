(function (window) {
    if (! window.AudioPlayer) {
        console.log('You need to plug in AudioPlayer first');
        return false;
    }

    window.Visualizer = function Visualizer(audioSourceElement, audioCtx, canvasElement) {

    };


})(window);
